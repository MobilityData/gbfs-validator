[build]
  command = "yarn build"
  publish = "website/dist"

[build.environment]
  YARN_FLAGS = "--frozen-lockfile --ignore-scripts --debug"

[functions]
  directory = "functions"

[dev]
  command = "yarn run dev:website"
  autoLaunch = false
  functions = "functions"
