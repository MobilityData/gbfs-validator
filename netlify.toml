[build]
  command = "yarn build"
  publish = "website/dist"

[build.environment]
  YARN_FLAGS = "--frozen-lockfile --network-concurrency 1"

[functions]
  directory = "functions"

[dev]
  command = "yarn run dev:website"
  autoLaunch = false
  functions = "functions"
